# Copy this file to `.env` and fill in your key.
# `.env` is gitignored, so your key never gets committed.
# app-guru loads this automatically -- no need to `export` anything by hand.

# Your Anthropic API key (https://console.anthropic.com/settings/keys)
# This is the ONLY key app-guru needs. `mine` uses it both to search the
# web (Claude's built-in web_search tool) and to extract scored app ideas.
# (`trends` needs no key at all -- Google Trends' public endpoint is free.)
ANTHROPIC_API_KEY=
